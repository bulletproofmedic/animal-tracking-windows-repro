# Merge order

1. Validate this maintenance branch on Windows.
2. Merge this maintenance PR to public `main`.
3. Rebase any continuing diagnostic PR that changes `.github/workflows/windows-repro.yml`.
4. Resume PR #27 work only after the public workflow is adopted.
