# Stop condition

The maintenance task stops when one Windows run passes with the Node 24 action pins, exact shallow checkout, payload guard, environment record, and staged-reproducer behavior intact.
