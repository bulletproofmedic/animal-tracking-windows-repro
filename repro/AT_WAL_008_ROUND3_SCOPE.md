# AT-WAL-008 round-three sanitized producer validation

This diagnostic models the three producer-owned residual controls returned by the independent successor re-audit.

Private candidate identity at dispatch:

- branch context: `fix/at-wal-008-core-remediation`;
- commit: `d423d8049dfad75a269bf652866c4ff03fcd3fc0`;
- public evidence authority: synthetic producer diagnostic only.

The payload proves these correction classes without copying private source:

1. one stable configuration identity cannot occur at multiple revisions, regardless of input order;
2. repository materialization and selection-count preprocessing have deterministic cancellation checkpoints;
3. selection-count identities preserve entity type;
4. validation binds execution to an expected Git commit, tree, merge base, clean tracked worktree, and exact changed-path population;
5. mutation evidence is calculated from explicit per-mutant outcomes rather than emitted as a constant.

The payload contains only synthetic UUIDs, text, temporary Git repositories, and generated rows. It contains no private source, private Git history, configuration, credentials, coordinates, maps, media, databases, logs, backups, or production data.

A successful run is producer evidence only. It does not independently close findings, authorize merge, establish PR #17 persistence integration, authorize owner use, or establish release readiness.
