# AT-WAL-008 round-four sanitized producer validation

The filename is retained only for continuity with the closed round-three diagnostic branch. This content and its executable controls are the final round-four validation set.

Final private candidate identity at dispatch:

- branch context: `fix/at-wal-008-core-remediation`;
- commit: `c3617dfe4b3f80f3cf98d6533b1bf95ff5c57fe8`;
- fixed base: `82ffecfb1fe0cfe0980fced74823d09877f23d20`;
- public evidence authority: synthetic producer diagnostic only.

The payload models these round-four correction classes without copying private source:

1. every governed UUID-keyed population rejects one stable identity at multiple revisions in either order;
2. legitimate cross-entity UUID reuse remains distinct in selection-count reconciliation;
3. a typed cross-entity exclusion cannot be concealed by an untyped identity collision;
4. explicit checkpoints cover payload uniqueness, bounded chunk sorting, graph traversal, and validation after snapshot closure;
5. validation exceptions are preserved;
6. a real depth-one clone is unshallowed before exact commit, tree, merge-base, clean-worktree, and changed-path checks;
7. measured evidence is calculated from six explicit outcomes.

The payload contains only synthetic UUIDs, generated rows, temporary Git repositories, and public diagnostic text. It contains no private source, private Git history, configuration, credentials, coordinates, maps, media, databases, logs, backups, audit evidence, or production data.

A successful run is producer evidence only. It does not independently close findings, authorize private merge, establish PR #17 persistence integration, authorize owner use, or establish release readiness.
